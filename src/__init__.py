"""AI Project Prompt Generator — src package."""

import logging

# Set up a null handler so library users don't see "No handler found" warnings.
logging.getLogger(__name__).addHandler(logging.NullHandler())
